export const metadata = {
  title: 'Simple Chat - Flowstack SDK Example',
  description: 'A simple AI chat app built with Flowstack SDK',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body style={{ margin: 0, fontFamily: 'system-ui, sans-serif' }}>
        {children}
      </body>
    </html>
  );
}
